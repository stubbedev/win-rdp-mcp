# dependencies
node_modules/

# build output
dist/
/win-rdp-mcp
/win-rdp-mcp.exe
bin/win-rdp-mcp-native
bin/win-rdp-mcp-native.exe

# nix build output
result
result-*

# config & secrets — the example is committed, a real one is not
/win-rdp-mcp.toml
.env
